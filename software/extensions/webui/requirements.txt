# extensions/webui 完整依赖清单（默认启用的官方扩展）
# 基础运行依赖：
flask>=2.3.0
flask-cors>=4.0.0
waitress>=3.0.0
websockets>=12.0
requests>=2.28.0
bcrypt>=4.0.0              # 可选：仅验证遗留 bcrypt 哈希

# 可选功能（默认关闭：GraphQL / gRPC），启用对应功能时再装：
ariadne>=0.21.0
graphql-core>=3.2.0
grpcio>=1.60.0
grpcio-tools>=1.60.0
